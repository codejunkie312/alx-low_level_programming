#include "main.h"

/**
 * main - prints out its own name
 * @argc: argument count
 * @argv: arguments array
 *
 * Return: 0 always
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
